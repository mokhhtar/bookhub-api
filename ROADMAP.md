# خارطة طريق BookHub — يوليو 2026

خطة تنفيذ 9 أفكار: ثلاث ميزات NYT + ست تطويرات لصفحة التلخيص.
مرتبة في 4 مراحل حسب (العائد ÷ الجهد) والاعتماديات بين المهام، مع تجميع
كل تعديلات حقول الاستجابة في ترقيات كاش دفعية (bump واحد لكل مرحلة بدل
إبطال الكاش عدة مرات).

**قاعدة عامة لكل المراحل**: أي حقل جديد في استجابة `/summary` = رفع نسخة
`summary_vN` + إضافة الحقل في `github_publisher.py` + عرضه في الصفحتين
(الديناميكية `summary.html` والثابتة `_layouts/book.html`).

---

## المرحلة 1 — مكاسب سريعة بلا بنية جديدة (جلسة عمل واحدة)

### 1.1 قسم "NYT Bestsellers هذا الأسبوع" في الصفحة الرئيسية ⭐ الأولوية — ✅ أُنجز 2026-07-09
**الهدف**: صف أفقي متحرك (نفس تصميم similar-marquee) بأبرز كتب الأسبوع،
كل نقرة تفتح ملخصنا. محتوى يتجدد أسبوعيًا تلقائيًا.

**الخطوات**:
1. استخراج `_nyt_overview()` من `tools/summary.py` إلى وحدة جديدة
   `tools/nyt.py` (راوتر + دوال مشتركة)، مع إبقاء استيرادها في summary.
2. إضافة حقل `book_image` (غلاف NYT) و`list_name_encoded` إلى اللقطة
   المفلطحة — موجودان في استجابة overview ولا نحفظهما حاليًا.
   (يتطلب رفع مفتاح `nyt:overview:v1` إلى `v2`.)
3. نقطة جديدة `GET /nyt/weekly`: تعيد ~12 كتابًا من القوائم الرئيسية
   (Hardcover Fiction/Nonfiction، Paperback Trade Fiction، Advice، YA)
   مرتبة بالمرتبة. لا طلب NYT إضافي — نفس اللقطة اليومية المخزنة.
   دعم `OPTIONS`/`HEAD` الرخيصين (نفس درس `/daily` مع مراقبي التشغيل).
4. الواجهة `index.html`: قسم بعد Daily Picks بنفس نمط الشريط المتحرك،
   بطاقة = غلاف + عنوان + شارة "#N · قائمة X". الرابط بنمط
   `bookLink()` الموجود (`/summary/?q=..&author=..&direct=true`).
   تخزين sessionStorage باليوم كما في daily.
5. إضافة استدعاء `/nyt/weekly` إلى workflow التسخين `warm-daily.yml`.

**المخاطر**: أغلفة NYT أحيانًا غائبة → fallback لبطاقة نصية (نمط
similar-cover-empty موجود). | **الجهد**: ~نصف يوم.
**معيار النجاح**: القسم يظهر بمحتوى الأسبوع الحالي، والنقر يفتح الملخص
مباشرة، وgraceful عند غياب المفتاح (القسم يختفي).

### 1.2 "المزيد لهذا المؤلف" (Open Library — مجاني)
**الهدف**: صف ثالث تحت Similar Books يكمل ثلاثية الاستكشاف.

**الخطوات**:
1. `resolve_author_works(record, limit=8)` في `tools/summary.py`:
   `openlibrary.org/search.json?author=NAME&sort=readinglog&limit=12&fields=title,author_name,cover_i,first_publish_year,key`
   — استبعاد الكتاب الحالي وأعمال بلا غلاف، بنفس فلترة
   `is_companion_material` المستخدمة في similar.
2. **كسل التحميل (lazy)**: لا يدخل في مهام `/summary` الـ12 — نقطة منفصلة
   `GET /author/works?name=..&exclude=..` تستدعيها الواجهة بعد عرض
   الملخص (لا تبطئ الاستجابة الرئيسية، ولا bump للكاش). كاش 30 يومًا.
3. الواجهة: بطاقة "More by {author}" بشبكة بسيطة (ليست marquee — تمييز
   بصري عن similar)، النقر يفتح الملخص مباشرة.
4. الصفحات الثابتة: تُترك لمرحلة لاحقة (تتطلب حقلًا في الـfront matter
   وبump — تُجمع مع 1.3 إن أردنا).

**الجهد**: ~ساعتان. | **معيار النجاح**: مؤلف غزير (Stephen King) يعرض
8 أعمال حقيقية بأغلفة؛ مؤلف مغمور يخفي القسم.

### 1.3 إحصاءات الطبعات والترجمات (Open Library)
**الهدف**: سطر مصداقية في الصفحة الجانبية: "🌍 120 طبعة · تُرجم إلى 40 لغة".

**الخطوات**:
1. `resolve_editions_stats(record)`: يتطلب `open_library_work_key` —
   `/works/{key}/editions.json?limit=1000&fields=languages` (أو حقلا
   `edition_count` و`language` من search.json — أرخص: طلب واحد، جرّب
   الاثنين وقس). يعيد `{"editions": N, "languages": M}`.
2. حدّ أدنى للعرض (طبعتان+ أو لغتان+) — كتاب بطبعة واحدة لا يستفيد.
3. يدخل ضمن مهام `/summary` (طلب واحد رخيص) + شفاء ذاتي كالعادة +
   **bump واحد `summary_v12` مشترك مع أي حقل آخر من هذه المرحلة**.
4. العرض: شارة في sidebar-badges + سطر في الصفحة الثابتة + publisher.

**الجهد**: ~ساعتان. | **معيار النجاح**: Harry Potter يعرض عشرات اللغات؛
كتاب حديث النشر لا يعرض شيئًا مضللًا.

### 1.4 زر "أضف لمكتبتي"
**الهدف**: ربط صفحة التلخيص بأداتي My Library وReading Tracker الموجودتين.

**الخطوات**:
1. فحص مخطط تخزين My Library الحالي (localStorage في صفحة المكتبة) —
   الالتزام بنفس المفاتيح والبنية حرفيًا.
2. زر في الصفحة الجانبية تحت Buy on Amazon: يحفظ
   {title, author, cover_url, isbn, google_id, added_at} + حالة
   "أُضيف ✓" إن كان موجودًا + toast تأكيد برابط "افتح مكتبتي".
3. رابط اختياري "ابدأ تتبع القراءة" يفتح Reading Tracker مع
   page_count معبأ مسبقًا (query params).
4. الصفحات الثابتة: نفس الزر بجافاسكربت خفيف في `book.html`.

**الجهد**: ~3 ساعات (أغلبها فحص المخطط الحالي). | **بلا أي باك-إند.**
**معيار النجاح**: كتاب مضاف من صفحة التلخيص يظهر في My Library فورًا.

---

## المرحلة 2 — صفحات SEO الجديدة (جلسة عمل واحدة)

### 2.1 صفحة "أرقام 1 الحالية" (/bestsellers/)
**الهدف**: متصدرو كل قوائم NYT (~18 كتابًا) في صفحة واحدة تتجدد أسبوعيًا.

**الخطوات**:
1. `GET /nyt/leaders` في `tools/nyt.py`: rank==1 من كل قائمة (نفس
   اللقطة المخزنة، صفر طلبات إضافية).
2. صفحة Jekyll جديدة `bestsellers.html` بالهيكل + JS يملأ الشبكة
   (نفس نمط بطاقات similar). كل بطاقة → ملخصنا.
3. **قيد معروف**: المحتوى client-rendered → فهرسة أضعف من static.
   القرار الواعي: نبدأ هكذا (بساطة)، وإن أثبتت الصفحة قيمتها نرقيها
   لاحقًا لنشر أسبوعي static عبر workflow (يلتزم data file في bookhub).
4. إضافة الصفحة إلى التنقل الرئيسي وsitemap.

**الجهد**: ~3 ساعات. | **معيار النجاح**: 18 متصدرًا بأغلفة، تحديث تلقائي
أسبوعيًا، meta tags كاملة.

### 2.2 شارات NYT على صفحات التصنيفات
**الهدف**: شارة "🏆 على قائمة NYT حاليًا" على بطاقات الكتب المطابقة.

**الخطوات**:
1. خريطة ثابتة NYT list_name_encoded ← تصنيفاتنا في `taxonomy.py`
   (hardcover-fiction/paperback-trade-fiction ← فئات الأدب؛
   advice-how-to ← self-help؛ young-adult-hardcover ← YA... إلخ).
2. صفحات التصنيفات static تعرض بطاقات الكتب المنشورة — الشارة تُحقن
   بـJS: fetch `/nyt/weekly` مرة واحدة، مطابقة normalized title+author
   مع `data-title`/`data-author` على البطاقات (تُضاف للقالب إن لم تكن).
3. الشارة عنصر صغير أعلى الغلاف، لا يغيّر التخطيط.

**الجهد**: ~ساعتان. | **معيار النجاح**: كتاب موجود في تصنيفنا وعلى قائمة
NYT حاليًا يحمل الشارة؛ لا شارات خاطئة (المطابقة صارمة كالباك-إند).

---

## المرحلة 3 — تجربة الاستخدام العميقة (2-3 جلسات)

### 3.1 بثّ الملخص تدريجيًا (Streaming) ⭐ أعلى أثر UX
**الهدف**: النص يبدأ بالظهور خلال ثانيتين بدل سبينر 15-20 ثانية.

**الخطوات**:
1. `gemini_client.generate_stream()` عبر
   `client.models.generate_content_stream` (نفس الإعدادات).
2. نقطة جديدة `POST /summary/stream` (الحالية تبقى كما هي للتوافق):
   - كاش موجود؟ → حدث واحد بالحمولة كاملة فورًا.
   - لا؟ → تبدأ بث نص الملخص chunks أولًا (SSE بنمط
     `data: {"t": "..."}`)، وبالتوازي تعمل بقية المهام الـ11؛ عند
     الاكتمال حدث أخير `data: {"done": {...payload كامل...}}` ثم
     التخزين في الكاش كالمعتاد (نفس المفتاح — الطريقان يتشاركان الكاش).
3. الواجهة: fetch + ReadableStream، عرض تدريجي في `.ai-output`
   (parseSummaryMarkdown على النص المتراكم)، ثم ملء الأقسام الجانبية
   من حدث done. fallback تلقائي للنقطة القديمة عند فشل البث.
4. **تحقق مبكر (spike)**: بروكسي Render يمرر SSE دون تجميع (البث يبقّي
   الاتصال حيًا فلا يصطدم بمهلة 100 ثانية — يُختبر أول شيء).

**المخاطر**: تعقيد أعلى تغيير في المشروع حتى الآن — يُنفّذ خلف مسار
منفصل حتى يثبت. | **الجهد**: يوم كامل. | **معيار النجاح**: أول حرف
< 3 ثوانٍ على كولد ستارت، والنتيجة النهائية مطابقة تمامًا للمسار القديم.

### 3.2 ملخصات بالعربية (تبديل لغة)
**الهدف**: نفس الملخص الموثّق بالعربية — جمهور شبه خالٍ من المنافسة.

**الخطوات**:
1. `SummaryRequest.language: str = "en"` (قيم مسموحة: en/ar فقط).
2. `_build_prompt(..., language)`: توجيه "اكتب بالعربية الفصحى" مع بقاء
   أسماء الأقسام HTML وبنيتها كما هي؛ **قواعد الحقيقة نفسها** (لا اختلاق).
3. مفتاح الكاش يتضمن اللغة. themes/reading_level تبقى إنجليزية v1
   (بيانات مشتركة) — الترجمة لاحقًا.
4. الواجهة: زر تبديل EN/ع فوق الملخص؛ عند العربية: `dir="rtl"` على
   `.ai-output` + خط مناسب + صوت TTS عربي (فحص توفر ar-* في
   speechSynthesis، إخفاء الزر إن غاب).
5. الصفحات الثابتة تبقى إنجليزية v1 (نشر /ar/ مسار SEO منفصل — خارج
   النطاق، يُقيّم بعد قياس الاستخدام).
6. **أنت المُقيّم**: قبل الإطلاق، 5 كتب متنوعة تراجع ملخصاتها العربية
   بنفسك (سلاسة، دقة مصطلحات، احترام قواعد عدم الاختلاق).

**الجهد**: نصف يوم + مراجعتك. | **معيار النجاح**: تبديل سلس دون إعادة
تحميل، RTL سليم في كل الأقسام، جودة لغوية تجيزها أنت.

### 3.3 قارئ داخلي للكتب المجانية
**الهدف**: قراءة كتب Gutenberg داخل الموقع بدل مغادرته.

**الخطوات**:
0. **Spike إلزامي أولًا**: هل يستطيع خادم Render جلب
   `gutenberg.org/ebooks/{id}.txt.utf-8`؟ (Gutendex حجبنا — الموقع الأم
   قد يحجب أيضًا). عند الحجب: mirror بديل
   (`aleph.gutenberg.org`, `gutenberg.pglaf.org`) أو Internet Archive.
   نتيجة الـspike تحدد الجدوى — **إن فشلت كل المصادر يُلغى البند**.
1. نقطة `GET /read/{gutenberg_id}/{page}`: جلب النص مرة واحدة، تقطيع
   ~4KB/صفحة، تخزين بنمط pdfchat الموجود (chunks في Redis عبر
   `set_key_strict`)، إرجاع صفحة + إجمالي الصفحات. حد أقصى ~2MB/كتاب.
2. صفحة `reader.html` في bookhub: عرض نصي نظيف (خط قراءة، عرض 65ch،
   وضع ليلي موروث من الثيم)، أزرار السابق/التالي + حفظ الموضع
   localStorage + رابط من زر "Read online" الحالي (يتحول للقارئ
   الداخلي عندما يتوفر، مع إبقاء رابط Gutenberg الخارجي كخيار).
3. صفحة القارئ خفيفة SEO-wise (noindex — نص Gutenberg منسوخ لا نريد
   فهرسته كمحتوى مكرر).

**الجهد**: يوم كامل (بعد نجاح الـspike). | **معيار النجاح**: فتح
Moby Dick والتنقل بين صفحاته والعودة لنفس الموضع، بلا تجاوز حدود Upstash.

---

## المرحلة 4 — مؤجّل عمدًا
- **NYT Article Search** (أخبار المؤلفين): تفعيل منفصل + حصص إضافية،
  العائد لا يبرر التعقيد الآن. يُعاد التقييم إن أصلحت NYT نقاطها المعطوبة
  (history/reviews) فتفتح "تاريخ الكتاب الكامل في القوائم" و"مراجعات NYT".
- **نشر static لصفحة /bestsellers/** و**صفحات /ar/**: بعد قياس استخدام
  نسختيهما الأوليين.

---

## ملاحظات تنفيذ عرضية
- **ترقيات الكاش**: المرحلة 1 = bump واحد (`summary_v12`) يجمع editions
  stats. البث والعربية يضيفان أبعادًا للمفتاح في المرحلة 3. لا bump
  للمهام الكسولة (author works) ولا لميزات الواجهة الصرفة.
- **الترتيب داخل كل مرحلة**: كما هو مرقّم — كل بند مستقل قابل للشحن وحده.
- **الاختبار**: كل بند باك-إندي يُختبر محليًا ثم على الخادم الحي قبل
  الانتقال للتالي (درس Gutendex: ما يعمل محليًا قد يُحجب من Render).
